# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=207beafabe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "GetTransactions_post_others.csv"
    fields:
      - "customerId"
      - "numOfTransactions"
      - "startDate"
      - "endDate"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "GetTransactions failure scenarios"
    flow:
      - log: "Invalid payload structure with missing required accountNumber"
      - post:
          url: "/GetTransactions"
          headers:
            botId: "example-botId"
            accountId: "example-accountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "example-X-AuthToken"
            koreUserId: "example-koreUserId"
            X-CORRELATION-ID: "12345"
          json:
            customerId: "{{ customerId }}"
            numOfTransactions: "{{ numOfTransactions }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
      - think: 1
      
      # Invalid Authorization token
      - log: "Invalid Authorization token in the header"
      - post:
          url: "/GetTransactions"
          headers:
            botId: "example-botId"
            accountId: "example-accountId"
            Authorization: "InvalidToken"
            X-Auth-token: "example-X-AuthToken"
            koreUserId: "example-koreUserId"
            X-CORRELATION-ID: "12345"
          json:
            customerId: "{{ customerId }}"
            accountNumber: ["{{ customerId }}"]
            numOfTransactions: "{{ numOfTransactions }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount:
              amount: 1000
              currency: 'USD'
          expect:
            - statusCode: 401
            - hasProperty: 'error'
      - think: 1
      
      # Incorrect header missing the required X-CORRELATION-ID
      - log: "Missing required X-CORRELATION-ID header"
      - post:
          url: "/GetTransactions"
          headers:
            botId: "example-botId"
            accountId: "example-accountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "example-X-AuthToken"
            koreUserId: "example-koreUserId"
          json:
            customerId: "{{ customerId }}"
            accountNumber: ["{{ customerId }}"]
            numOfTransactions: "{{ numOfTransactions }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount:
              amount: { "gt": 1000 }
              currency: 'USD'
          expect:
            - statusCode: 400
            - hasProperty: 'error'
      - think: 1

      # Invalid URL test
      - log: "Validation failure due to invalid URL"
      - post:
          url: "/InvalidURL"
          headers:
            botId: "example-botId"
            accountId: "example-accountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "example-X-AuthToken"
            koreUserId: "example-koreUserId"
            X-CORRELATION-ID: "12345"
          json:
            customerId: "{{ customerId }}"
            accountNumber: ["{{ customerId }}"]
            numOfTransactions: "{{ numOfTransactions }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount:
              amount: 1000
              currency: 'USD'
          expect:
            - statusCode: 404
            - hasProperty: 'error'
      - think: 1

