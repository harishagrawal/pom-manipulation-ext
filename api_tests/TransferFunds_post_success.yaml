# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /TransferFunds_post for http method type POST in artillery framework
# RoostTestHash=289ed66c26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/TransferFunds_post_success.csv"
    fields:
      - "koreUserId"
      - "amount"
      - "currency"
      - "customerId"
      - "date"
      - "sourceAccountId"
      - "targetAccountId"
      - "description"
      - "TransactionType"
      - "PaymentType"
    skipHeader: true
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  plugins:
    expect: {}

scenarios:
  - name: "TransferFunds Successful Scenario"
    flow:
      - log: "Sending a valid TransferFunds request to transfer funds between accounts"
      - post:
          url: "/TransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "fixed_bot_id"
            userCode: "fixed_user_code"
            accountId: "fixed_account_id"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            date: "{{ date }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 200
          # Expected behavior: The server performs the transfer successfully and returns a 200 status code.
      - think: 1

