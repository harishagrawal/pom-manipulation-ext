# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /deposit_post for http method type POST in artillery framework
# RoostTestHash=70db6d5eac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "deposit_post_success.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true
  defaults:
    headers:
      koreUserId: "test-user-id" # Example static header value
  plugins:
    expect: {}

scenarios:
  - name: "Deposit API test scenario"
    flow:
      - post:
          url: "/deposit"
          headers:
            botId: "test-bot-id" # Example static header value
            accountId: "test-account-id" # Example static header value
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          log: "Making a successful deposit with the provided payload."
          think: 1
          expect:
            - statusCode: 200
              hasProperty: [] # Update with actual property names from response, if applicable
      # Include additional requests following the same pattern as needed

