import csv
import random
import faker
from datetime import datetime, timedelta

# Initialize Faker library
fake = faker.Faker()

# Define variables
fields = ['koreUserId', 'botId', 'accountId', 'Authorization', 'X-Auth-token', 'X-CORRELATION-ID', 'customerId', 'accountNumber', 'numOfTransactions', 'status', 'startDate', 'endDate', 'sort', 'transactionAmount', 'category', 'debitOrCredit']

# Create list of dictionaries with dummy data
data = []
for _ in range(100):  # Generating 100 rows of data
    data.append({
        'koreUserId': fake.uuid4(),
        'botId': fake.uuid4(),
        'accountId': fake.uuid4(),
        'Authorization': fake.sha256(),
        'X-Auth-token': fake.sha1(),
        'X-CORRELATION-ID': fake.uuid4(),
        'customerId': fake.random_number(digits=5),
        'accountNumber': fake.random_number(digits=10),
        'numOfTransactions': random.randint(1, 100),
        'status': random.choice(['Active', 'Inactive']),
        'startDate': (fake.date_between(start_date='-30d', end_date='today')).strftime('%Y-%m-%d'),
        'endDate': (fake.date_between(start_date='today', end_date='+30d')).strftime('%Y-%m-%d'),
        'sort': random.choice(['asc', 'desc']),
        'transactionAmount': round(random.uniform(1, 10000), 2),
        'category': random.choice(['Groceries', 'Utilities', 'Dining', 'Transport']),
        'debitOrCredit': random.choice(['Debit', 'Credit']),
    })

# Write data to CSV file
with open('dummy_data.csv', 'w', newline='') as csvfile:
    writer = csv.DictWriter(csvfile, fieldnames=fields)
    writer.writeheader()
    for row in data:
        writer.writerow(row)
