# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=8a95fbc531

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "GetAccountInfo_post_others.csv"
    fields:
      - "customerId_invalid"
      - "botId_invalid"
      - "accountId_invalid"
      - "koreUserId_invalid"
      - "X_CORRELATION_ID_invalid"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "GetAccountInfo error handling"
    flow:
      - log: "Sending request with invalid Authorization token"
      - post:
          url: "/GetAccountInfo"
          headers:
            Content-Type: "application/json"
            koreUserId: "{{ koreUserId_invalid }}"
            botId: "{{ botId_invalid }}"
            accountId: "{{ accountId_invalid }}"
            Authorization: "invalid {{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X_AUTH_TOKEN }}"
            X-CORRELATION-ID: "{{ X_CORRELATION_ID_invalid }}"
          json:
            customerId: "{{ customerId_invalid }}"
          expect:
            - statusCode: 401
            - hasProperty: 'err'
            - hasProperty: 'message'

      - think: 1

      - log: "Sending request to invalid URL (not found)"
      - post:
          url: "/InvalidGetAccountInfoUrl"
          headers:
            Content-Type: "application/json"
            koreUserId: "{{ koreUserId_invalid }}"
            botId: "{{ botId_invalid }}"
            accountId: "{{ accountId_invalid }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X_AUTH_TOKEN }}"
            X-CORRELATION-ID: "{{ X_CORRELATION_ID_invalid }}"
          json:
            customerId: "{{ customerId_invalid }}"
          expect:
            - statusCode: 404
            - hasProperty: 'err'
            - hasProperty: 'message'

