# Test generated by RoostGPT for test api-artillery-azure using AI Type Azure Open AI and AI Model roostgpt-4-32k

# Test generated for /GetAccountInfo_post for http method type POST in artillery framework
# RoostTestHash=8a95fbc531

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    fields:
      - customerId
    path: "GetAccountInfo_post_success.csv"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: Test Get Account Information
    flow:
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "user102"
            botId: "bot-1234"
            accountId: "account_567"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "auth-token-abcd"
            X-CORRELATION-ID: "azure-123"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "accounts"
          log: "Post request successful, fetched customer account details"
      - think: 1
