# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4


# Test generated for /GetAccountInfo_post for http method type POST in artillery framework
# RoostTestHash=8a95fbc531

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetAccountInfo_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "X-Auth-token"
      - "X-CORRELATION-ID"
      - "customerId"
    skipHeader: true
scenarios:
  - name: "Get Account Info Scenario"
    flow:
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "accounts"
          capture:
            - json: "$.customerId"
              as: "customerId"
            - json: "$.accounts"
              as: "accounts"
          log: "Posted Account Info"
      - think: 1
