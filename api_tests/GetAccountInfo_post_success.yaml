# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /GetAccountInfo_post for http method type POST in artillery framework
# RoostTestHash=8a95fbc531

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    - path: "/mnt/data/GetAccountInfo_post_success.csv"
      fields:
        - "koreUserId"
        - "botId"
        - "accountId"
        - "customerId"
      skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
    - log: "Sending a valid request to get customer account details with expected success response."
    - post:
        url: "/GetAccountInfo"
        headers:
          koreUserId: "{{ koreUserId }}"
          botId: "{{ botId }}"
          accountId: "{{ accountId }}"
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
          X-CORRELATION-ID: 123456789
        json:
          customerId: "{{ customerId }}"
        expect:
          - statusCode: 200
          - hasHeader: "content-type"
          - hasProperty: "accountId"
      think: 1
    - log: "Sending request with invalid authorization token, expecting a 401 response."
    - post:
        url: "/GetAccountInfo"
        headers:
          koreUserId: "{{ koreUserId }}"
          botId: "{{ botId }}"
          accountId: "{{ accountId }}"
          Authorization: "InvalidToken"
          X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
          X-CORRELATION-ID: 123456789
        json:
          customerId: "{{ customerId }}"
        expect:
          - statusCode: 401
          - hasHeader: "content-type"
          - hasProperty: "err.message"
      think: 1
    - log: "Sending request with non-existing account, expecting a 404 response due to account not found."
    - post:
        url: "/GetAccountInfo"
        headers:
          koreUserId: "{{ koreUserId }}"
          botId: "{{ botId }}"
          accountId: "{{ accountId }}"
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
          X-CORRELATION-ID: 987654321
        json:
          customerId: 999999999
        expect:
          - statusCode: 404
          - hasHeader: "content-type"
          - hasProperty: "err.message"
      think: 1

