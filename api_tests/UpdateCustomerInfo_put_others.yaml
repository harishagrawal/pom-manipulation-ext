# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=89ddb90785

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/UpdateCustomerInfo_put_others.csv"
    fields:
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update Customer Info - Negative Test Cases"
    flow:
      - think: 2

      - comment: "Test: Sending request with missing authorization header should fail with 401"
      - put:
          url: "/UpdateCustomerInfo"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          headers:
            koreUserId: "some-user-id"
            botId: "some-bot-id"
            accountId: "some-account-id"
            X-Auth-token: "VALID_TOKEN"
            X-CORRELATION-ID: 123456
          log: "Missing Authorization header should result in 401"
          expect:
            - statusCode: 401
            - hasProperty: 'err'

      - think: 2

      - comment: "Test: Sending request with invalid authorization token should fail with 401"
      - put:
          url: "/UpdateCustomerInfo"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          headers:
            koreUserId: "some-user-id"
            botId: "some-bot-id"
            accountId: "some-account-id"
            Authorization: "InvalidAuthToken"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: 123456
          log: "Invalid authorization token should result in 401"
          expect:
            - statusCode: 401
            - hasProperty: 'err'

      - think: 2

      - comment: "Test: Sending request with invalid X-CORRELATION-ID header format should fail with 404"
      - put:
          url: "/UpdateCustomerInfo"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          headers:
            koreUserId: "some-user-id"
            botId: "some-bot-id"
            accountId: "some-account-id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "VALID_TOKEN"
            X-CORRELATION-ID: "INVALID_CORRELATION_ID" # Sending invalid data type
          log: "Invalid X-CORRELATION-ID header format should result in 404"
          expect:
            - statusCode: 404
            - hasProperty: 'err'

      - think: 2

      - comment: "Test: Sending request with invalid URL should fail with 404"
      - put:
          url: "/InvalidUpdateCustomerInfoURL" # Intentionally invalid URL
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          headers:
            koreUserId: "some-user-id"
            botId: "some-bot-id"
            accountId: "some-account-id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "VALID_TOKEN"
            X-CORRELATION-ID: 123456
          log: "Invalid API endpoint URL should result in 404"
          expect:
            - statusCode: 404
            - hasProperty: 'err'

