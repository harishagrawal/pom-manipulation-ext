# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /GetScheduleTransfers_post for http method type POST in artillery framework
# RoostTestHash=7b4d2d87f9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/GetScheduleTransfers_post_success.csv"
    fields:
      - "customerId"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Get Scheduled Transfers"
    flow:
      - get:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "test-user-id"
            botId: "test-bot-id"
            userCode: "test-user-code"
            accountId: "test-account-id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Fetching all scheduled transfers with pending status"
          think: 1
          expect:
            - statusCode: 200
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "DueDate"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "ScheduledDate"
            - hasProperty: "NextScheduleDate"
            - hasProperty: "id"

      # Expected to fail due to missing Authorization
      - get:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "test-user-id"
            botId: "test-bot-id"
            userCode: "test-user-code"
            accountId: "test-account-id"
          log: "Attempting to fetch scheduled transfers without proper authorization should fail"
          think: 1
          expect:
            - statusCode: 401

