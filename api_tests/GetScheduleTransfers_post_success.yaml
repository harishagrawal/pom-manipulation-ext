# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4


# Test generated for /GetScheduleTransfers_post for http method type POST in artillery framework
# RoostTestHash=7b4d2d87f9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetScheduleTransfers_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
      - "customerId"
      - "status"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Validate GetScheduleTransfers API"
    flow:
      - post:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "{{koreUserId}}"
            botId: "{{botId}}"
            userCode: "{{userCode}}"
            accountId: "{{accountId}}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{customerId}}"
            status: "{{status}}"
          beforeRequest:
            - log: "Executing API request to fetch scheduled transfers"
          expect:
            - statusCode: 200
            - hasProperty: ['Type', 'CustomerId', 'Amount', 'SourceAccountNumber', 'TargetAccountNumber', 'DueDate', 'Currency', 'Status', 'ScheduledDate', 'NextScheduleDate', 'id']
            - hasHeader: "Content-Type"
          afterResponse:
            - log: "API request to fetch scheduled transfers completed"
        think: 1
