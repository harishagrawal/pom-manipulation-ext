# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /UpdateScheduleTransfers_put for http method type PUT in artillery framework
# RoostTestHash=c0505709a3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/UpdateScheduleTransfers_put_success.csv"
    fields:
      - "paymentId"
      - "customerId"
      - "amount"
      - "scheduledDate"
      - "sourceAccountId"
      - "targetAccountId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update Scheduled Transfers Test"
    flow:
      - post:
          url: "/UpdateScheduleTransfers"
          headers:
            koreUserId: "test_koreUserId" # Sample header
            botId: "{{ botId }}"  # Required header
            userCode: "test_userCode" # Sample header
            accountId: "{{ accountId }}" # Required header
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}" # Authorization token from environment
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            scheduledDate: "{{ scheduledDate }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
          log: "Sending request to update a scheduled transfer with customer Id {{ customerId }}"
          think: 2
          expect:
            - statusCode: 200
            - hasProperty: 'success' # example property to check in the response
            - hasHeader: 'Content-Type'
      - log: "Scheduled transfer update request completed."

