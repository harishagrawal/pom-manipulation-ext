# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /DeleteScheduleTransfers_post for http method type POST in artillery framework
# RoostTestHash=47355f872f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/DeleteScheduleTransfers_post_success.csv"
    fields:
      - "paymentId"
      - "updateTransferType"
      - "customerId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete scheduled transfer"
    flow:
      - comment: "Attempting to delete a single scheduled transfer"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "some-kore-user-id"
            botId: "{{ $processEnvironment.BOT_ID }}"
            userCode: "some-user-code"
            accountId: "{{ $processEnvironment.ACCOUNT_ID }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasHeader: 'Content-Type'
          think: 1

      - comment: "Attempting to delete all scheduled transfers for a customer"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "another-kore-user-id"
            botId: "{{ $processEnvironment.BOT_ID }}"
            userCode: "another-user-code"
            accountId: "{{ $processEnvironment.ACCOUNT_ID }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            updateTransferType: "all"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasHeader: 'Content-Type'
          think: 1

