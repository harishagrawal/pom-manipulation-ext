# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4


# Test generated for /DeleteScheduleTransfers_post for http method type POST in artillery framework
# RoostTestHash=47355f872f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "DeleteScheduleTransfers_post_success.csv"
    fields:
      - "paymentId"
      - "updateTransferType"
      - "customerId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "DeleteScheduleTransfers scenario"
    flow:
      - post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
          log: "Delete the scheduled transfer"
        think: 1
