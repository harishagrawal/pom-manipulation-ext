# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=88810bf424

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/GetCustomerInfo_post_others.csv"
    fields:
      - "customerId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "GetCustomerInfo - Error and Edge Case Testing"
    flow:
      - log: "Sending request with missing required headers to expect 404 Not Found"
        post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: 0
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
        comment: "Expect 404 Not Found due to missing required headers"

      - think: 1

      - log: "Sending request with invalid Authorization to expect 401 Unauthorized"
        post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "InvalidToken"
            botId: "wrongBotId"
            accountId: "wrongAccountId"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err"
            - hasProperty: "err.message"
        comment: "Expect 401 Unauthorized due to invalid Authorization header"

      - think: 1

      - log: "Sending request with wrong 'koreUserId' header type to expect 404 Not Found"
        post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: 123456
            botId: "validBotId"
            accountId: "validAccountId"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
        comment: "Expect 404 Not Found due to wrong 'koreUserId' header type"

      - think: 1

      - log: "Sending request with invalid 'X-CORRELATION-ID' to expect 404 Not Found"
        post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "notANumber"
            botId: "validBotId"
            accountId: "validAccountId"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
        comment: "Expect 404 Not Found due to invalid 'X-CORRELATION-ID' header value"

      - think: 1

      - log: "Sending request with invalid payload to expect 404 Not Found"
        post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "validBotId"
            accountId: "validAccountId"
          json:
            invalidCustomerId: 1045300
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
        comment: "Expect 404 Not Found due to invalid payload structure"

      - think: 1

      - log: "Sending request with invalid URL to expect 404 Not Found"
        post:
          url: "/InvalidEndpoint"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "validBotId"
            accountId: "validAccountId"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
        comment: "Expect 404 Not Found due to invalid URL"

