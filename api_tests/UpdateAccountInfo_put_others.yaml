# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4

# RoostTestHash=adb57fbf1b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "UpdateAccountInfo_put_others.csv"
    fields: ["koreUserId", "botId", "accountId", "X-Auth-token", "X-CORRELATION-ID", "accountNumber", "customerId", "email", "alternatePhoneNumber", "isAlternateMobileNumber", "phone", "isPrimaryMobileNumber", "location", "isStatement"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "UpdateAccountInfo API failure and edge case testing"
    flow:
      - post:
          url: "/UpdateAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
          log: "Testing authorization failure case, expecting 401 status code"
        think: 1
      - post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
          log: "Testing invalid URL case, expecting 404 status code"
        think: 1
