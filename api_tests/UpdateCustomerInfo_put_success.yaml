# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /UpdateCustomerInfo_put for http method type PUT in artillery framework
# RoostTestHash=89ddb90785

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "/mnt/data/UpdateCustomerInfo_put_success.csv"
    fields:
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
    skipHeader: true
  environments:
    staging:
      API_HOST: "https://api.staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Scenario to update customer information with various payloads"
    flow:
      - log: "Updating customer information with a valid payload"
      # Sending a request to update customer information
      - put:
          url: "/UpdateCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ xAuthToken }}"
            X-CORRELATION-ID: "{{ xCorrelationId }}"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 200
            - header: {"Content-Type": "application/json"} # Check that response contains a header "Content-Type"
      - think: 1

      - log: "Attempting to update customer information without required fields"
      # Attempting to update customer info without required fields should result in 401 authorization failure
      - put:
          url: "/UpdateCustomerInfo"
          headers:
            koreUserId: "missing"
            botId: "missing"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "missing"
            X-CORRELATION-ID: "{{ xCorrelationId }}"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 401
            - header: {"Content-Type": "application/json"} # Check that response contains a header "Content-Type"
      - think: 1

      - log: "Updating customer information with another set of payload data"
      # Sending another request with different set of valid payload data
      - put:
          url: "/UpdateCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ xAuthToken }}"
            X-CORRELATION-ID: "{{ xCorrelationId }}"
          json:
            customerId: "{{ customerId }}"
            email: "changedemail@example.com"
            alternatePhoneNumber: 9876543210
            isAlternateMobileNumber: false
            phone: 1234567890
            isPrimaryMobileNumber: true
            location: "Different Location"
          expect:
            - statusCode: 200
            - header: {"Content-Type": "application/json"} # Check that response contains a header "Content-Type"
      - think: 1

