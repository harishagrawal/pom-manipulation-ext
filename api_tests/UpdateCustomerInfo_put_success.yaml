# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateCustomerInfo_put for http method type PUT in artillery framework
# RoostTestHash=89ddb90785

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "UpdateCustomerInfo_put_success.csv"
    fields:
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update customer information"
    flow:
      - post:
          url: "/UpdateCustomerInfo"
          headers:
            koreUserId: "sampleUserId"
            botId: "sampleBotId"
            accountId: "sampleAccountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "sampleAuthToken"
            X-CORRELATION-ID: 123456
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "email"
            - hasProperty: "alternatePhoneNumber"
            - hasProperty: "isAlternateMobileNumber"
            - hasProperty: "phone"
            - hasProperty: "isPrimaryMobileNumber"
            - hasProperty: "location"
          log: "Update customer information request sent successfully"
      - think: 1
