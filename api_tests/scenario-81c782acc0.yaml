# ********RoostGPT********

# Test generated by RoostGPT for test karate-integration using AI Type Open AI and AI Model gpt-4
# 
# {
#   background: 'Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: 'Get all products\r\n' +
#     '    When the client sends a GET request "/api/products" to get the list of all products\r\n' +
#     '    Then the list of products returned should be empty',
#   title: 'Get all products'
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}

scenarios:
  - name: "Get all products"
    flow:
      - log: "Sending GET request to /api/products to get the list of all products"
      - get:
          url: "/api/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "0"
            - hasProperty: "0.id"
            - hasProperty: "0.name"
            - hasProperty: "0.description"
            - hasProperty: "0.price"
            - equals:
                0: []
        think: 1
