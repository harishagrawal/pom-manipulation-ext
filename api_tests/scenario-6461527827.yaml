# ********RoostGPT********

# Test generated by RoostGPT for test karate-integration using AI Type Open AI and AI Model gpt-4
# 
# {
#   background: 'Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: 'Delete an existing product\r\n' +
#     '    Given there is an existing product with ID 1\r\n' +
#     '    When the client sends a DELETE request to "/api/products/1"\r\n' +
#     '    Then the response status code should be 200\r\n' +
#     '    And the product with ID 1 should no longer exist',
#   title: 'Delete an existing product'
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "scenario-6461527827.csv"
    fields: ["productId"]
    skipHeader: true

scenarios:
  - name: Delete an existing product
    flow:
      - log: "Given there is an existing product with ID 1"
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - equals: 
                id: "{{ productId }}"
      - log: "When the client sends a DELETE request to /api/products/1"
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
      - think: 1
      - log: "Then the response status code should be 200"
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - log: "And the product with ID 1 should no longer exist"
