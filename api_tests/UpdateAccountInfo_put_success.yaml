# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /UpdateAccountInfo_put for http method type PUT in artillery framework
# RoostTestHash=adb57fbf1b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/UpdateAccountInfo_put_success.csv"
    fields:
      - "accountNumber"
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
      - "isStatement"
    skipHeader: true
  defaults:
    headers:
      koreUserId: "test-user"
      botId: "{{ botId }}"
      accountId: "{{ accountId }}"
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      X-Auth-token: "test-auth-token"
      X-CORRELATION-ID: 123456789
  plugins:
    expect: {}

scenarios:
  - flow:
      - post:
          url: "/UpdateAccountInfo"
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          log: "Updating customer account details with valid payload"
          # Expected behavior: API should return 200 status with a success message
          think: 1
          capture:
            json: "$.message"
            as: "response_message"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - header: "Content-Type"
      - log: "The response message was '{{ response_message }}'"

