# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateAccountInfo_put for http method type PUT in artillery framework
# RoostTestHash=adb57fbf1b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "UpdateAccountInfo_put_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "X-Auth-token"
      - "X-CORRELATION-ID"
      - "accountNumber"
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
      - "isStatement"
    skipHeader: true
scenarios:
  - name: "Update Account Information"
    flow:
      - post:
          url: "/UpdateAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          expect:
            - statusCode: 200
          log: "Updating account info"
      - think: 1
