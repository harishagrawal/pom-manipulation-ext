# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4

# RoostTestHash=289ed66c26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "TransferFunds_post_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
      - "customerId"
      - "amount"
      - "currency"
      - "sourceAccountId"
      - "targetAccountId"
      - "date"
      - "description"
      - "TransactionType"
      - "PaymentType"
    skipHeader: true

scenarios:
  - name: "TransferFunds failure cases"
    flow:
      - log: "Testing with invalid authorization token"
        post:
          url: "/TransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "invalid_token"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 401
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1

      - log: "Testing with missing required header parameters"
        post:
          url: "/TransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 400
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1

      - log: "Testing with invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1
