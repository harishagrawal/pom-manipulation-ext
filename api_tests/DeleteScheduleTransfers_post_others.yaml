# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47355f872f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "DeleteScheduleTransfers_post_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
      - "customerId"
      - "paymentId"
      - "updateTransferType"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "DeleteScheduleTransfers API failure and edge cases"
    flow:
      - log: "Invalid authorization Test"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
      - log: "Invalid URL Test"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1
      - log: "Missing required header Test"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
      - think: 1
      - log: "Missing required payload Test"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
