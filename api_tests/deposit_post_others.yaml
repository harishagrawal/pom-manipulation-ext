# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70db6d5eac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "deposit_post_others.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Deposit API negative tests"
    flow:
      - log: "Sending request with an invalid authorization header to test authorization failure."
      # Expecting authorization failure
      - post:
          url: "/deposit"
          headers:
            botId: "test-bot"
            accountId: "test-account"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN_INVALID }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 401
            - hasProperty: "err"

      - think: 1

      - log: "Sending request with missing mandatory headers to test handling of incomplete requests."
      # Expecting Not Found response due to missing mandatory headers
      - post:
          url: "/deposit"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"

      - think: 1
      
      - log: "Sending request to an invalid URL to test response for non-existing endpoints."
      # Expecting Not Found response due to an invalid URL
      - post:
          url: "/invalid_deposit_url"
          headers:
            botId: "test-bot"
            accountId: "test-account"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"

      - think: 1

      - log: "Sending request with an invalid 'accountId' header value to test input validation."
      # accountId header value is invalid
      - post:
          url: "/deposit"
          headers:
            botId: "test-bot"
            accountId: "invalid-accountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"


