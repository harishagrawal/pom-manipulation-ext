# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70db6d5eac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "deposit_post_others.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "deposit API test"
    flow:
      - log: "Testing with invalid headers"
        post:
          url: "/deposit"
          headers:
            koreUserId: "test"
            botId: "test"
            accountId: "test"
            Authorization: "InvalidToken"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 401
            - hasProperty: "err"
            - hasProperty: "err.message"
        think: 1
      - log: "Testing with invalid URL"
        post:
          url: "/depositInvalid"
          headers:
            koreUserId: "test"
            botId: "test"
            accountId: "test"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
        think: 1
