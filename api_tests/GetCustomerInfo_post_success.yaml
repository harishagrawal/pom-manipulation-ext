# Test generated by RoostGPT for test gpt4-restA-koreBank using AI Type Open AI and AI Model gpt-4


# Test generated for /GetCustomerInfo_post for http method type POST in artillery framework
# RoostTestHash=88810bf424

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetCustomerInfo_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "X-Auth-token"
      - "X-CORRELATION-ID"
      - "customerId"
    skipHeader: true
  plugins:
    expect: {}
    
scenarios:
  - name: "Customer Information"
    flow:
      - post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "alternatePhoneNumber"
            - hasProperty: "customerId"
            - hasProperty: "email"
            - hasProperty: "identificationNumber"
            - hasProperty: "identificationType"
            - hasProperty: "image"
            - hasProperty: "isAlternateMobileNumber"
            - hasProperty: "isPrimaryMobileNumber"
            - hasProperty: "location"
            - hasProperty: "name"
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "personalBanker"
            - hasProperty: "phone"
            - hasProperty: "securityQuestions"
            - hasProperty: "segmentId"
            - hasProperty: "userId"
            - hasProperty: "id"
          log: "Posted a request to /GetCustomerInfo"
      - think: 1
