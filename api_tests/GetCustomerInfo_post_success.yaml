# Test generated by RoostGPT for test gpt-turbo-artill-korebank using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /GetCustomerInfo_post for http method type POST in artillery framework
# RoostTestHash=88810bf424

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "/mnt/data/GetCustomerInfo_post_success.csv"
    fields:
      - "customerId"
      - "koreUserId"
      - "botId"
      - "accountId"
      - "X_Auth_token"
      - "X_CORRELATION_ID"
    skipHeader: true

scenarios:
  - flow:
      - post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X_Auth_token }}"
            X-CORRELATION-ID: "{{ X_CORRELATION_ID }}"
          json:
            customerId: "{{ customerId }}"
          log: "Fetching customer information with given customerId."
          # Successful retrieval of customer information
          think: 1
          expect:
            - statusCode: 200
            - hasProperty: 'customerId'
            - hasProperty: 'email'
            - hasProperty: 'identificationNumber'
            - hasProperty: 'isAlternateMobileNumber'
            - hasProperty: 'name'
            - hasHeader: 'Content-Type'
      - post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "some-user-id"
            botId: "some-bot-id"
            accountId: "some-account-id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "some-auth-token"
            X-CORRELATION-ID: 987654321
          json:
            customerId: 112233
          log: "Fetching customer information with fixed customerId."
          # Successful retrieval of customer information with fixed data
          think: 1
          expect:
            - statusCode: 200
            - hasProperty: 'email'
            - hasProperty: 'phone'
            - hasProperty: 'segmentId'
            - hasHeader: 'Content-Type'

